You are performing a documentation-only pass on this repository.

NON-NEGOTIABLE RULES

1. Do not modify runtime code, tests, package manifests, lockfiles, build configuration, CI configuration, dependencies, generated files, or deployment configuration unless this prompt explicitly requests a documentation-only link or metadata correction.
2. Do not invent capabilities, guarantees, APIs, states, ownership boundaries, implementation status, security properties, protocol behaviour, or future commitments.
3. Treat accepted ADRs, exported public types, source code, tests, and the current README as evidence. In that order, prefer implemented and tested behaviour over prose.
4. If sources disagree, do not silently resolve the conflict. Add a clearly labelled "Documentation conflict" or "Open question" section and report it in the final summary.
5. Clearly distinguish: implemented and tested; implemented but not integration-tested; scaffolded; designed; proposed; explicitly out of scope.
6. Never describe proposed or scaffolded behaviour as currently working.
7. Do not add marketing language or unsupported claims such as "secure", "production-ready", "guaranteed", "zero trust", or "enterprise-grade".
8. Keep terminology consistent with project-runtime-contracts and accepted ecosystem terminology.
9. Use relative repository links.
10. Mermaid diagrams may be added only when every component, relationship, and transition is supported by repository evidence or an explicit requirement in this prompt.
11. Keep changes restricted to Markdown or plain-text documentation files and README documentation links.
12. Do not create empty placeholder files. Every created document must contain useful, repository-specific content.
13. Do not duplicate the README. New documentation should deepen, formalise, or operationalise material that is too detailed for the README.
14. Do not commit or push unless explicitly instructed. Prepare the changes and report the diff.

EVIDENCE AND WORKFLOW

Before editing:
- Inspect the repository tree.
- Read the README, existing docs, ADRs, public types, package boundaries, and relevant tests.
- List the evidence sources you will use.
- Identify contradictions, outdated statements, naming inconsistencies, and unresolved decisions.
- Confirm that the requested documents are not already present under another name.

During editing:
- Preserve existing accepted decisions.
- Prefer short normative statements for rules and detailed explanatory sections for rationale.
- Add links from the README or docs index only where useful.
- Use exact paths, type names, outcome names, and identifiers found in the repository.
- When referring to another repository, state clearly whether the dependency is current, optional, planned, or blocked.

After editing:
- Show every created and modified file.
- Summarise the purpose of each file.
- List every unresolved question or documentation conflict.
- List any wording that required inference rather than direct extraction.
- Run available Markdown formatting, lint, or link checks if already configured.
- Show `git diff --stat`.
- Show the documentation-only diff or a concise section-by-section summary.
- Confirm that no runtime code or configuration changed.


PROJECT: Project Ananke
REPOSITORY: https://github.com/hourwise/Project-Ananke

PRIMARY GOAL
Make Ananke's authority model, threat model, execution lifecycle, retry semantics, recovery behaviour, and integration contract explicit without overstating security guarantees.

AUTHORITY BOUNDARY
Ananke owns proposal assessment, risk classification, policy decisions, approval requirements, approval binding/invalidation, governed tool routing, typed outcomes, and authoritative audit for governed action decisions and attempts.
Ananke does not own project memory truth, memory scoring, general orchestration, IDE process security, model transport, bypass credentials, or actions executed through ungoverned paths.
Preserve this limitation: Ananke governs only actions and credentials exclusively routed through it.

REQUESTED DOCUMENTS
1. `docs/threat-model.md`
   Include scope, assets, trust boundaries, trusted components, untrusted inputs, attacker capabilities, threats, mitigations, residual risks, evidence, and open questions. Cover direct-tool/credential bypass, approval substitution, post-approval mutation, replay, stale state, confused deputy, malicious metadata, prompt/tool injection, cross-server exfiltration, read-only poisoning, audit tampering, operator/dashboard compromise, denial of service, partial-success ambiguity, and insecure topology where relevant.
2. `docs/integration/gateway-contract.md`
   Formalise proposal -> canonicalisation -> policy classification -> approval requirement -> approval presentation -> binding -> execution -> outcome -> audit. For each stage define owner, input, output, invariant, identifiers, invalidation, and retryability. Use exact repository outcome and reason-code names.
3. `docs/operations/failure-recovery.md`
   Explain agent/operator behaviour for COMPLETED, FAILED, DENIED, WAITING_FOR_APPROVAL, APPROVAL_INVALIDATED, STALE_STATE, TIMED_OUT, and PARTIAL_SUCCESS: meaning, safe next action, retry rules, reapproval, refresh, and audit expectations.
4. `docs/operations/retry-and-idempotency.md`
   Document retry ownership, idempotency, replay protection, timeout, duplicates, partial success, refresh, approval reuse/prohibition, cancellation, and operator intervention. Mark unresolved behaviour instead of prescribing it.
5. `docs/security/deployment-assumptions.md`
   State assumptions for exclusive credentials/tool routing, network exposure, local/remote deployment, dashboard authentication, audit protection, process identity, filesystem permissions, sensitive logs, and terminal/extension bypass.
6. `docs/decisions/README.md`
   Create/update the ADR index.

SPECIAL REVIEW QUESTIONS
Do not silently decide: approval expiry basis; identifier reuse on retries; automatic retry of partial success; cancellation of in-flight external actions; whether append-only audit is implementation or policy; canonicalisation version binding; policy-change invalidation; sensitivity of read-only tools; sanitisation of untrusted tool metadata.

README CHANGES
Add concise links and strengthen the bypass limitation only where needed. Do not duplicate existing laws or architecture.

DELIVERABLE STANDARD
An integrator should be able to answer: What is governed? What bypasses it? What is approval bound to? When is it invalidated? What does each outcome require next? Which retries are safe? What evidence supports each claim?
